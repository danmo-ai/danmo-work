# Volume outline — v{{NN}} {{卷名}}

**卷纲只写卷级判断 + 单元索引 + 本卷人物。卷纲分配，细纲落笔。** 单元级细节（desire/obstacle/choice/payoff/pleasure/forbidden/scenes/章切口/on_stage/pov）**全部在 `outline/units/v{{NN}}-U#.yaml`**，本文件不重复。

批准后跑 `novel_gate.py --action accept-volume --volume v{{NN}}`：「本卷人物」全部 `candidate → canon`，并按单元索引每行种出 `outline/units/v{{NN}}-U#.yaml` 头（status: proposed）。

## 锁卷 checklist（批准前勾选）

- [ ] 本卷目标一句话立住；与全书读者承诺不打架
- [ ] 长线结构钩已写；短线不空窗过久；设了必有兑或可见回馈
- [ ] 单元索引因果链不断（每个 unit_id 有入口 + 一句话功能 + 下钩类型）
- [ ] 「本卷人物」列全（stem 与 `canon/cast/*.md` 文件名一致；细纲 `on_stage` 只能从这里选）
- [ ] 有双线时：事业/感情各一句 + 本卷交织点；无则标「跳过」
- [ ] 终局边界已填；未提前打光圣经禁碰
- [ ] 连续 3 个单元主爽点形态不雷同（在 yaml 的 `pleasure` 字段核对）
- [ ] 单元按「一次矛盾从起到收」切开；章数落在 3–8（硬上限 10）。高潮/中点落在某单元的决断或兑现上

## 卷目标

一句话：本卷结束时主角必须得到/失去什么。

### 结构钩（期待灯塔；不定稿钩子文案）

- 长线（本卷主灯塔，可跨单元）：
- 短线（近期可兑现；可并行 1–2 条）：
- 本卷须兑现/抬升的旧承诺（无则写无）：

### 双线（有则填；无感情主线写「跳过」）

- 事业/升级线（本卷推进到哪）：
- 感情/关系线（本卷推进到哪）：
- 交织点（挂哪个单元 ID；禁止双线平推互不咬合）：

### 地图（可选）

本卷主舞台 → 可扩至：（中期疲软时可换地图抬矛盾）。这里只标舞台，不列章内场景。

## 核心冲突

- 对手/阻力：
- 代价：
- 对立升级（本卷层级）：

## 起终状态

- 开卷：谁 / 处境 / 能力
- 卷终：谁 / 处境 / 能力
- 成长节点（2–4 个，跨多章，不是每章一条）：

## 终局边界（对照圣经 / 总纲）

- 本卷可解锁：
- 本卷禁碰：（同步登记到 `canon/locked-terms.yaml` 的 `locked_until`）

## 节奏锚点

节点：目标 → 冲突 → 中点 → 高潮 → 章位。锚点章须落在某单元的「决断 / 兑现」节拍上。

- 本章范围：ch–ch
- 开篇切口：ch
- 中点反转：ch
- 卷高潮章位：ch
- 卷末收束：ch

## 单元索引（只索引，不展开）

每个单元一行索引。先定本卷高潮，再按「一次矛盾从起到收」往前切；切完再数章（建议 3–8，硬上限 10）。节奏锚点是卷的幕，一个锚点可以跨多个单元，不另做幕索引。章内场景不写在本表，细纲里叫场面。

**单元级合同**（desire/obstacle/choice/payoff/pleasure/forbidden/scenes/chapters）只写在 `outline/units/v{{NN}}-U#.yaml`，本文件不重复。

| unit_id | 章范围 | 一句话功能 | 本单元终局边界（禁碰） | 下一单元钩子类型 |
|---------|--------|------------|------------------------|------------------|
| v{{NN}}-U1 | ch–ch | | | 信息缺口 |
| v{{NN}}-U2 | ch–ch | | | 高代价选择 |
| v{{NN}}-U3 | ch–ch | | | 倒计时 |
| v{{NN}}-U4 | ch–ch | | | 未兑现承诺 |
| v{{NN}}-U5 | ch–ch | | | 身份反转 |
| v{{NN}}-U6 | ch–ch | | | 关系临界 |
| v{{NN}}-U7 | ch–ch | | | 未完成动作 |
| v{{NN}}-U8 | ch–ch | | | 突然揭示 |

**规则（gate 对照）：**
- 章范围必须连续、覆盖本卷全部章，无重叠无缺口（`accept-volume` / `lint-units` blocking）。
- 钩子类型只用九类：信息缺口 / 未兑现承诺 / 高代价选择 / 身份反转 / 关系临界 / 倒计时 / 未完成动作 / 突然揭示 / 回声/意象。细纲 `next_hook.type` 须与本表一致（blocking）；具体事件文案只在 yaml `next_hook.out`。
- 一句话功能由本表定，`accept-volume` 种进细纲 `function`；细纲改了只 warning，以卷纲为准。
- 终局边界格非空时，细纲 `forbidden` 与 `endgame_boundary` 不得皆空（blocking）。
- 单元之间必须有因果（上一单元的 next_hook 引出下一单元的 entry）。
- 连续 3 个单元主爽点形态雷同 → 重排（在 yaml `pleasure` 核对）。
- **禁止**在本文件写 desire/obstacle/choice/payoff/pleasure/forbidden/scenes/场面/章切口——那些是 yaml 的活。

## 本卷人物（stem；批准即 canon）

一行一个 `canon/cast/<stem>.md` 的文件名。细纲 `on_stage` 只能从这里选；`accept-volume` 把这里的 `candidate` 全部提成 `canon`。龙套不列。

- {{protagonist-stem}}
- {{antagonist-stem}}

## 情绪与人物弧

- 卷情绪：开卷 → 中点 → 高潮（各一词：爽/虐/慌/暖/悚/窘）
- 人物弧（只写本卷变化）：`谁: 从X→Y`

## 反转

本卷 1–3 个，挂在锚点上。类型：信息 / 身份 / 立场。须可回看。

| 锚点 | 类型 | 读者误以为 | 实际是 |
|------|------|------------|--------|
| 中点 / 高潮 | | | |

## 伏笔

- 本卷回收：FS-
- 本卷新埋：FS-
